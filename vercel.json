{
  "version": 2,
  "builds": [
    { "src": "api/index.js", "use": "@vercel/node" },
    { "src": "client", "use": "@vercel/static-build" }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "command": "npm install && npm install --prefix api && npm install --prefix client && npm run build --prefix client"
  }
}
